{% block content %}
    <div class='col-md-5'>
        {{ form_start(form) }}
            {{ form_widget(form) }}

            <button class="btn btn-danger">{{ button_label|default("S'inscrire") }}</button>
        {{ form_end(form) }}
    </div>
{% endblock %}
